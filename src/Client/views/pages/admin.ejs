<!DOCTYPE html>
<html dir="rtl">
<head>
    <title>VCS Admin Page</title>
    <% include ../partials/head %>
</head>
<body>
<header>
    <% include ../partials/header %>
</header>
<section class="jumbotron text-center">
    <div class="container">
        <h1 class="jumbotron-heading"><%= strings.admin %></h1>
        <p class="lead text-muted"><%= strings.adminDesc %></p>
    </div>
</section>


<div class="album text-muted center">
    <div class="container">
        <ul class="nav nav-tabs">
            <li class="nav-item">
                <a class="nav-link active" id="addRoom-tab" data-toggle="tab" href="#addRoom" role="tab"
                   aria-controls="home" aria-selected="true">
                    <%= strings.addRoom %></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" id="addClass-tab" data-toggle="tab" href="#addClass" role="tab"
                   aria-controls="profile" aria-selected="false">
                    <%= strings.addClass %></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" id="addUserToRoom-tab" data-toggle="tab" href="#addUserToRoom" role="tab"
                   aria-controls="profile" aria-selected="false">
                    <%= strings.addUserToRoom %></a>
            </li>
        </ul>

        <div class="tab-content" id="myTabContent" align="center" style="padding-top: 10px">
            <div class="tab-pane fade show active" id="addRoom" role="tabpanel" aria-labelledby="addRoom-tab">
                <b><%= strings.addRoom %>:</b> <br/>
            </div>
            <div class="tab-pane fade" id="addClass" role="tabpanel" aria-labelledby="addClass-tab">
                <b><%= strings.addClass %>:</b> <br/>
                <form>
                    <div class="form-group">
                        <label for="exampleInputEmail1"><%= strings.className %></label>
                        <input type="text" class="form-control" name="className" placeholder="<%= strings.className %>">

                    </div>
                    <div class="form-group">
                        <label for="exampleInputPassword1"><%= strings.description %></label>
                        <input type="text" class="form-control" name="desc" placeholder="<%= strings.description %>">
                    </div>
                    <button type="submit" class="btn btn-primary"><%= strings.addClass %></button>
                </form>
            </div>
            <div class="tab-pane fade" id="addUserToRoom" role="tabpanel" aria-labelledby="addUserToRoom-tab">
                <b><%= strings.addUserToRoom %>:</b> <br/>
            </div>
        </div>

    </div>
</div>

<footer>
    <% include ../partials/footer %>
</footer>
</body>
</html>